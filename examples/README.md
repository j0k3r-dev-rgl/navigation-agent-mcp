# Examples

Carpeta con proyectos pequeños para probar `find_symbol`, `trace_flow`, `trace_callers` y otras capacidades con casos reales controlados.

## Lenguajes

- `go/` — app pequeña con handlers, services y repository
- `python/` — espacio reservado para ejemplos Python
